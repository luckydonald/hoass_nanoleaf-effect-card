# The query for the AI to work with.

Generate me a Homeassistant plugin based on the following description. 
- If the plugin requires a frontend, use Vue, TS, and SCSS for that.
- If the plugin requires a backend, use modern Python 3.12+ for that, including proper type hinting!
- For type-hinting, import types from Homeassistant where possible (both frontend and backend).
